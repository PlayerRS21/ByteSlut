# ByteSlut — Python dependencies
# Install with: pip install --break-system-packages -r requirements.txt
# Or let install.sh handle it automatically

flask>=3.0.0          # Web dashboard (tiny, fast Python web framework)
psutil>=5.9.0         # System stats: CPU, RAM, disk, network, processes
dbus-python>=1.3.2    # DBus for notifications + session events
PyGObject>=3.44.0     # GLib event loop (needed by dbus-python)

# Optional but recommended:
# xprintidle          # Idle time on X11 — install via: sudo pacman -S xprintidle
